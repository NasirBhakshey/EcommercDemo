spring:
  config:
    import: ""

  cloud:
    config:
      enabled: false

    discovery:
      enabled: false

  flyway:
    enabled: false

  kafka:
    listener:
      auto-startup: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false